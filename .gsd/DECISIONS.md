# Decisions Register




| #    | When     | Scope      | Decision                                           | Choice                                                                                                                                                                          | Rationale                                                                                                                                                                                                                                                               | Revisable?                                             |
| ---- | -------- | ---------- | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| D001 | M001     | arch       | Agent harness                                      | Claude Code CLI only (headless mode)                                                                                                                                            | Simplest integration, no API key management in driftless, well-documented headless/print mode                                                                                                                                                                           | Yes — when adding other harnesses (R028)               |
| D002 | M001     | arch       | Toolchain                                          | Vite+ (`vp`) as unified toolchain                                                                                                                                               | One tool replaces turborepo + tsup + vitest + biome/eslint/prettier. Rust-based performance. Manages pnpm + node.                                                                                                                                                       | No                                                     |
| D003 | M001     | arch       | Monorepo structure                                 | pnpm workspaces with packages/ and apps/ dirs                                                                                                                                   | Clean separation: packages/cli, packages/core, packages/action, apps/web, apps/docs                                                                                                                                                                                     | No                                                     |
| D004 | M001     | convention | Module format                                      | ESM-first with CJS compat where needed                                                                                                                                          | Modern standard, TypeScript strict mode                                                                                                                                                                                                                                 | No                                                     |
| D005 | M001     | convention | CLI prompts library                                | @clack/prompts                                                                                                                                                                  | Vercel-style UX, polished interactive prompts, cancellation handling                                                                                                                                                                                                    | Yes — if UX requirements change                        |
| D006 | M001     | convention | Doc output default                                 | Plain markdown with YAML frontmatter                                                                                                                                            | Widely supported by docs frameworks. Framework-specific adapters (fumadocs MDX, docusaurus MDX) available as options.                                                                                                                                                   | No                                                     |
| D007 | M001     | arch       | E2E test interpretation                            | Agent-driven (no custom parsers)                                                                                                                                                | Claude Code interprets any test framework. Framework-agnostic by design — no maintenance burden of parser per framework.                                                                                                                                                | No                                                     |
| D008 | M001     | scope      | v1 boundary                                        | E2E tests ↔ training docs only                                                                                                                                                  | Sharp product boundary. Broader doc automation (API docs, README gen) deferred to future milestones. The constraint is the feature.                                                                                                                                     | Yes — when pursuing R029                               |
| D009 | M001     | convention | Config file                                        | .driftless.json in target repo root                                                                                                                                             | Persists all init choices. Bridge between CLI and GitHub Actions.                                                                                                                                                                                                       | No                                                     |
| D010 | M001     | convention | Skill install location                             | .skills/ in target repo (user-configurable)                                                                                                                                     | Default location matches emerging convention. User can override during init.                                                                                                                                                                                            | Yes — if community feedback suggests different default |
| D011 | M001     | arch       | Init failure handling                              | Rollback + debug log                                                                                                                                                            | Fail-clean: no partial state. .driftless/debug.log for issue reporting. Init is idempotent.                                                                                                                                                                             | No                                                     |
| D012 | M002     | arch       | GitHub Action inference                            | anthropics/claude-code-action@v1                                                                                                                                                | Standard maintained integration. No custom API wiring. Requires ANTHROPIC_API_KEY in user's repo secrets.                                                                                                                                                               | Yes — when adding other harnesses                      |
| D013 | M002     | arch       | Capability modularity                              | Independent composable modules                                                                                                                                                  | E2e test writer and doc generator are independent. User picks one or both. Actions respect this choice via .driftless.json.                                                                                                                                             | No                                                     |
| D014 | project  | convention | Web search tool                                    | google_search (not search-the-web/search_and_read)                                                                                                                              | User preference. Note in PROJECT.md and agents config.                                                                                                                                                                                                                  | No                                                     |
| D015 | project  | convention | GitHub UI operations                               | agent-browser native skill                                                                                                                                                      | For repo settings, topics, branch protection, etc.                                                                                                                                                                                                                      | No                                                     |
| D016 | project  | scope      | OSS skill learning                                 | Incremental during execution                                                                                                                                                    | Build ~/.gsd/agent/skills/oss-repo-setup/ from real experience shipping driftless, not front-loaded research.                                                                                                                                                           | No                                                     |
| D017 | M001/S01 | convention | Library build command                              | `vp pack` (not `vp build`) for library packages                                                                                                                                 | `vp build` is the Vite application builder (dev server + Rolldown). Library packages use `vp pack` (tsdown). Root "build" script runs `vp run -r build` where each package script calls `vp pack`.                                                                      | No                                                     |
| D018 | M001/S01 | convention | Vite+ installation                                 | `npm install -g vite-plus` (not curl installer)                                                                                                                                 | The `curl -fsSL https://vite.plus | bash` installer returned HTTP 403. Global npm install works and provides `vp` binary. CI should use npm/pnpm global install too.                                                                                                    | Yes — if official installer is fixed                   |
| D019 | M001/S01 | convention | DTS generation                                     | `dts: true` (not `dts: { tsgo: true }`)                                                                                                                                         | The scaffold's default `tsgo: true` requires `@typescript/native-preview` which fails when resolved from global `vp`. Plain `dts: true` uses standard TypeScript for declaration generation.                                                                            | Yes — when tsgo stabilizes                             |
| D020 | M001/S01 | convention | Package tsconfig shape                             | Independent per-package tsconfigs (not extending root)                                                                                                                          | Matches Vite+ scaffold convention. Root tsconfig uses `nodenext` module resolution for root-level files; packages use `preserve`/`bundler` for tsdown compatibility. Different purposes = different configs.                                                            | No                                                     |
| D021 | M001/S01 | convention | Format/lint config                                 | Standalone `.oxfmtrc.json` + `.oxlintrc.json` (no root vite.config.ts)                                                                                                          | oxfmt and oxlint can't load `.ts` config on Node 20 (requires Node 22.6+ native TS). Removed root `vite.config.ts` and use dedicated JSON configs instead. `vp check` passes cleanly.                                                                                   | Yes — when Node 22+ is baseline                        |
| D022 | M001/S01 | convention | CLI auto-invocation                                | `main()` called at module level in index.ts                                                                                                                                     | `node packages/cli/dist/index.mjs` must print version per slice contract. Tests mock `console.log` before dynamic import to handle the auto-invocation. Keeps bin entry simple (single file).                                                                           | No                                                     |
| D023 | M001/S02 | convention | CLI arg parsing                                    | Set-based argv parsing (no flag library)                                                                                                                                        | Sufficient for 3 flags (`--version`, `--help`, `--dry-run`) and 1 positional command (`init`). No dependency needed at this scale.                                                                                                                                      | Yes — if commands grow past ~5                         |
| D024 | M001/S02 | convention | Config atomic writes                               | Write to `.driftless.tmp.json` then `fs.rename`                                                                                                                                 | Crash-safe: interrupted writes leave a temp file (diagnosable) instead of corrupting the config. S05 rollback can build on this pattern.                                                                                                                                | No                                                     |
| D025 | M001/S02 | convention | Config error diagnostics                           | `Error.cause` chaining with failure mode in message                                                                                                                             | `readConfig` throws with file path + failure type (missing vs. parse) + original error in `.cause`. Agents and users get full context without unwinding.                                                                                                                | No                                                     |
| D026 | M001/S02 | convention | Init command loading                               | Dynamic `import()` for initCommand from main entry                                                                                                                              | Keeps `--version` and `--help` fast — no prompt library loaded until `init` is actually invoked.                                                                                                                                                                        | No                                                     |
| D027 | M001/S03 | arch       | Adapters as prompt templates                       | System prompt fragments, not post-processors                                                                                                                                    | Including format instructions in the agent prompt produces correctly-formatted output natively. Post-processing markdown→MDX with regex is fragile. Light normalization only.                                                                                           | No                                                     |
| D028 | M001/S03 | arch       | One agent invocation per file                      | `claude -p` called once per test file                                                                                                                                           | Simplest, most testable strategy. Each file is a discrete unit. Avoids multi-doc parsing from a single invocation. "Agent decides" grouping deferred.                                                                                                                   | Yes — if batch mode proves needed                      |
| D029 | M001/S03 | convention | Flat adapters module                               | Single `adapters.ts` file (not `adapters/` directory)                                                                                                                           | Three functions returning prompt strings don't warrant separate files. Split later if adapters grow complex.                                                                                                                                                            | Yes — if adapters gain state or complexity             |
| D030 | M001/S03 | convention | docGrouping defaults to per-file                   | Optional field, no prompt, defaults to `"per-file"`                                                                                                                             | Only tested strategy for v1. "Agent decides" mode flagged as risky. Prompt can be added when other modes are proven.                                                                                                                                                    | Yes — when adding grouping modes                       |
| D031 | M001/S03 | convention | Glob resolution with minimatch                     | Added as explicit devDep, bundled by vp pack                                                                                                                                    | Node 20 lacks built-in glob. Transitive availability is fragile. minimatch is tiny, well-tested, and gets tree-shaken into the bundle.                                                                                                                                  | Yes — when Node 22+ baseline has fs.glob               |
| D032 | M001/S05 | convention | Debug log format                                   | JSON array (not JSON-lines)                                                                                                                                                     | Simpler parsing with `JSON.parse()`. Each init run overwrites the log. Acceptable for v1 where init runs infrequently.                                                                                                                                                  | Yes — if append semantics needed                       |
| D033 | M001/S05 | convention | Rollback strategy                                  | Pre-existence flag, not content snapshots                                                                                                                                       | FileTransaction records `{ path, type, preExisted }`. Rollback skips pre-existing files entirely. No content backup — init targets fresh directories, so overwrite-and-restore isn't needed.                                                                            | Yes — if init touches existing files                   |
| D034 | M001/S05 | convention | Init test strategy                                 | Real temp dirs with real transaction/logger                                                                                                                                     | Integration tests use `mkdtemp` with real `FileTransaction` and `DebugLogger`. Only heavy subsystems (agent, skills, detection) are mocked. Gives true filesystem coverage of rollback and debug log paths.                                                             | No                                                     |
| D035 | M002     | arch       | Workflow templates as parameterized YAML functions | Same pattern as skill templates in `skills.ts` — functions in `workflows.ts` that take `DriftlessConfig` and return YAML strings                                                | Consistent with M001's template pattern (D027). YAML is the product, not runtime code. Templates scaffolded at init time, not generated at Action runtime.                                                                                                              | No                                                     |
| D036 | M002     | arch       | Staleness detection delegated to prompt            | Claude reasons from `git diff` → affected features → stale docs. No custom diff-to-doc mapping code.                                                                            | Consistent with D007 (agent-driven interpretation). The prompt instructs the reasoning chain; skill files provide domain structure. Custom code would be fragile and less accurate than LLM inference.                                                                  | Yes — if accuracy proves insufficient                  |
| D037 | M002     | convention | PR summary comment in workflow prompt              | Workflow prompt instructs Claude to comment on the PR with a summary of changes made                                                                                            | Addresses open question from M002-CONTEXT.md. Standard practice for bot-assisted PRs. Visibility into what the action changed without reading the diff.                                                                                                                 | Yes — if users find it noisy                           |
| D038 | M002     | convention | Two-slice order: doc-update first                  | S01 builds doc-update workflow + all shared patterns; S02 adds test-gen reusing those patterns                                                                                  | Doc-update is the core "can't drift" promise, higher risk (staleness detection), and establishes all shared YAML infrastructure. Test-gen is structurally identical with a different prompt.                                                                            | No                                                     |
| D039 | M002/S01 | convention | installWorkflows mirrors installSkills pattern     | `installWorkflows(config, options)` in `workflows.ts` follows same signature/shape as `installSkills` — takes config + `{ cwd, dryRun }`, returns `{ installed, workflowsDir }` | Consistent API surface. Init command can call both the same way. Transaction registration follows same pattern.                                                                                                                                                         | No                                                     |
| D040 | M002/S01 | convention | Workflow YAML helpers exported for S02 reuse       | `permissionsBlock()`, `checkoutStep()`, `forkDetectionStep()`, `apiKeyCheckStep()`, `botLoopCondition()` exported from `workflows.ts`                                           | S02's `testGenWorkflowTemplate` shares all operational edge handling. Exporting fragment-builders avoids duplication while keeping each template function as the assembler of its own YAML.                                                                             | No                                                     |
| D041 | M002/S02 | convention | `getWorkflowFilenames` helper for init dry-run     | Exported function instead of importing `WORKFLOW_TEMPLATES` constant in init.ts                                                                                                 | vitest's mock proxy intercepts `in` operator and property access on mocked modules. A function survives the `...actual` spread in `vi.mock` cleanly, while constants accessed with `in`/`[]` trigger mock boundary errors. Simpler to test, same data-driven guarantee. | No                                                     |
| D042 | M003     | scope      | Package naming: scoped `@driftless-ai/*`           | `@driftless-ai/cli` for CLI package, `@driftless-ai/core` for core. `@driftless` org was unavailable on npm; user created `@driftless-ai` org.                                   | Unscoped `driftless` is taken on npm (setInterval replacement, 4+ years old). Scoped gives a namespace for future packages. `npx @driftless-ai/cli init` works.                                                                                                         | Yes — if unscoped name becomes available               |
| D043 | M003     | arch       | Release strategy: simple tag-based publish          | `git tag v1.0.0 && git push --tags` triggers CI publish. No semantic-release, no changesets.                                                                                    | Solo-maintainer project with 2 packages. semantic-release adds monorepo plugin complexity. changesets adds manual changeset-file ceremony. Simple tag→publish is the right automation level for v1. Can adopt semantic-release later if release frequency demands it.     | Yes — if release volume increases                      |
| D044 | M003     | convention | CHANGELOG v1.0.0: hand-written                     | v1.0.0 CHANGELOG is a hand-written summary of M001+M002 work. Future releases use conventional commits + tooling.                                                               | Existing commit history is close to conventional but not strictly formatted. Retroactive parsing would produce noisy/incomplete output. Clean summary for v1 is better than messy automation.                                                                            | No                                                     |
| D045 | M003     | convention | Slice ordering: publish-first, then parallel        | S01 (publish) proves the gate. S02 (CI), S03 (community), S04 (auto-update) are independent after S01 — can run in parallel.                                                    | Everything depends on proven npm publishing: CI automates it, README links to it, auto-update checks it. Maximizes parallelism after the highest-risk slice.                                                                                                            | No                                                     |
| D046 | M003     | convention | npm provenance on publish                           | Publish with `--provenance` flag in CI release workflow                                                                                                                         | Links published package to CI build for supply chain security. Free, one flag, standard practice for 2025+. Requires `id-token: write` permission in GitHub Actions.                                                                                                    | No                                                     |
| D047 | M003/S02 | convention | Release notes via body_path                         | Write CHANGELOG extract to temp file, pass via `body_path` to softprops/action-gh-release                                                                                      | Inline `body:` fails with multiline markdown containing shell-special characters. `body_path` reads from file, bypassing all escaping issues.                                                                                                                           | No                                                     |
| D048 | M003/S02 | convention | Self-contained release workflow                     | Release workflow re-runs check/test/build instead of depending on CI having passed                                                                                              | Tag pushes can bypass branch protection. Re-running ensures no broken publish even if CI was somehow skipped. Small cost (minutes) for high confidence.                                                                                                                 | No                                                     |
| D049 | M003/S03 | convention | Issue templates: YAML form schema                   | YAML form schema with typed fields (dropdown, input, textarea) and required validation — not freeform markdown templates                                                        | Structured forms produce consistent, machine-parseable issue data. Freeform markdown templates are routinely ignored or half-filled. YAML forms enforce required fields at submission time.                                                                              | No                                                     |
| D050 | M003/S03 | convention | Branch protection: classic rules                    | Classic branch protection rules on `main` (not repository rulesets)                                                                                                             | Simpler to configure and verify. Compatible with GitHub Free plan. Rulesets add org-level features not needed for a solo/small-team project. Can migrate to rulesets later if org-level policy is needed.                                                                | Yes — if org-level policy management needed            |
| D051 | M003/S04 | convention | No external semver library                          | 10-line `parseSemver` + `isNewerVersion` for exact `x.y.z` comparison                                                                                                          | Auto-update only needs exact version comparison, not range resolution or pre-release handling. Avoids adding `semver` (50KB+ dependency) for a trivial operation. If pre-release/range support is needed later, adopt the library then.                                  | Yes — if pre-release or range comparison needed        |
| D052 | M003/S04 | convention | Dynamic import for auto-update hook                 | `tryAutoUpdate()` uses `await import("@driftless-ai/core")` instead of top-level import                                                                                         | Keeps `--version` and `--help` fast (~66ms) by avoiding eagerly loading core + fetch + child_process modules. The hook only runs after fast-path exits, before command routing. Consistent with D026 (dynamic import for init command).                                  | No                                                     |
| D053 | M004     | arch       | Single Next.js app for landing page + docs          | Combined `apps/web` with fumadocs route groups: `app/(home)` for landing, `app/docs/[[...slug]]` for documentation. Not two separate apps.                                      | Fumadocs' recommended pattern. Eliminates domain/subdomain question (subpath only), simplifies to one Vercel project, shares design tokens via fumadocs-ui Tailwind preset. Sets up M005 naturally (add `/pricing` route). D003 listed `apps/web` + `apps/docs` separately — this updates that for M004. | Yes — if landing page needs separate deployment        |
| D054 | M004     | convention | Domain: `driftless.vercel.app` for v1               | Use Vercel's default subdomain, not a custom domain                                                                                                                             | Custom domain adds DNS configuration, certificate provisioning, and cost for a project that doesn't have users yet. Vercel subdomain is free, instant, and can be swapped to a custom domain later without changing any code.                                            | Yes — when custom domain is warranted                  |
| D055 | M004     | convention | Playbook format: standalone markdown                | Standalone markdown file at `~/Desktop/driftless/m004-launch-playbook.md` with pre-written tweet copy, Mermaid diagrams, timing tables                                          | Tool-agnostic — executable without Typefully, Buffer, or any paid scheduling tool. Markdown is portable, version-controllable, and can be copy-pasted into any platform. Typefully noted as optional enhancement.                                                        | Yes — if scheduling tool integration is added          |
| D056 | M004     | arch       | Slice ordering: integration-first, then parallel    | S01 proves Next.js + fumadocs + Vercel (ships landing page). S02 (docs content) and S03 (playbook) run in parallel after S01.                                                   | S01 retires the only real risk (new build tool in monorepo + deployment). S02 and S03 are independent creative work that benefits from having the live URLs from S01.                                                                                                    | No                                                     |
| D057 | M004/S01 | convention | Tailwind v4 CSS-first config for fumadocs           | Use CSS `@import` directives (`fumadocs-ui/css/neutral.css`, `fumadocs-ui/css/preset.css`) instead of JS `tailwind.config.js` with `createPreset`                               | Fumadocs v15+ recommends CSS-first Tailwind v4 setup. No JS config file needed. Simpler, aligns with Tailwind v4 conventions.                                                                                                                                           | No                                                     |
| D058 | M004/S01 | convention | apps/web excluded from vp check/test/build          | `apps/web` uses `next build` directly, not `vp pack` or `vp run`. Root `vp run -r build` may need adjustment or `apps/web` excluded from oxlint/oxfmt scope.                    | Next.js has its own build pipeline incompatible with `vp pack`. The existing `vp run -r build` orchestrates library packages; `apps/web` builds via `next build`. Lint/format tools may need ignore patterns for Next.js-specific syntax.                                | Yes — if vp adds Next.js support                       |
| D059 | M004/S01 | convention | fumadocs-mdx Source.files compatibility bridge      | `lib/source.ts` calls `files()` as a function if returned by fumadocs-mdx, since fumadocs-core v15.8 expects an array                                                          | fumadocs-mdx v11 returns `{ files: () => VirtualFile[] }` (lazy), but fumadocs-core v15.8 destructures `{ files }` and iterates it as an array. Bridge normalizes at the source loader call site. Remove when versions align.                                            | Yes — when fumadocs-mdx/core versions sync             |
| D060 | M004/S01 | convention | fumadocs page data typed as `any` for body/toc      | Docs page casts `page.data as any` to access `body` and `toc` MDX properties                                                                                                   | fumadocs-mdx provides `body` (FC) and `toc` (TableOfContents) at runtime, but the generic type chain through `createMDXSource` → `loader` loses them. Casting is the pragmatic fix. Remove when fumadocs types improve.                                                  | Yes — when fumadocs types carry MDX properties         |


| D061 | M004/S01 | design     | Editorial dark-luxury aesthetic for landing page     | Landing page uses Instrument Serif display font, amber/gold accents, left-aligned editorial hero, numbered feature rows, noise texture, gradient-fade dividers. Not the usual gradient-hero template. | frontend-design skill requires bold, distinctive aesthetic. Editorial/magazine direction chosen for developer-tool credibility. Instrument Serif provides character without sacrificing readability. Amber as accent color establishes brand identity without being garish. | No — brand direction is set                            |
| D062 | M004/S01 | convention | font-display Tailwind utility via --font-display var | Added `--font-display` CSS variable in `@theme inline` mapping to `var(--font-instrument)` so `font-display` Tailwind utility resolves to Instrument Serif                       | T01 used `font-display` class in JSX but only defined `--font-instrument` variable on the font. Tailwind v4 maps `font-{name}` to `--font-{name}`. The variable bridge ensures the serif font actually renders in headlines.                                             | No — standard Tailwind v4 theming                      |
| D063 | M004/S01 | ops        | Vercel monorepo deploy: rootDirectory apps/web      | Deploy from monorepo root with Vercel project configured via API: `rootDirectory: apps/web`, `framework: nextjs`, `installCommand: pnpm install`, `buildCommand: next build`. Vercel auto-detects pnpm from lockfile and runs install from repo root. | Deploying from `apps/web` directly failed (Vercel used npm). Deploying from root without rootDirectory failed (no framework detected). API configuration of project settings was the reliable path for pnpm monorepos. | No — standard Vercel monorepo pattern                  |
