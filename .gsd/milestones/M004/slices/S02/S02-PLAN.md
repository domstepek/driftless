# S02: Documentation content

**Goal:** All five required docs sections are complete — Quick Start (expanded), init walkthrough, GitHub Action setup, configuration reference, and troubleshooting — with accurate content derived from source code.
**Demo:** Visiting `/docs` shows a sidebar with all five pages, each containing accurate content that matches the actual v1.0.0 CLI behavior. Search finds content from every page.

## Must-Haves

- Quick Start page rewritten to fix `generate` standalone command reference and list all 6 init prompts accurately
- Init walkthrough page mirrors the exact prompt sequence from `init-prompts.ts` with realistic examples
- GitHub Actions page documents both workflow templates with operational edge explanations (fork detection, API key check, bot loop prevention)
- Configuration reference page documents every `DriftlessConfig` field from `types.ts` with type, default, and description
- Troubleshooting page covers common error paths from `config.ts`, `agent.ts`, and `logger.ts`
- `meta.json` controls sidebar ordering: Quick Start → Init Walkthrough → GitHub Actions → Configuration → Troubleshooting
- All pages use fumadocs MDX conventions from `content/docs/index.mdx`: YAML frontmatter with `title`/`description`, no manual `# Title` heading, `<Callout>` components
- Cross-links between pages use absolute paths (`/docs/configuration`, `/docs/troubleshooting`, etc.)
- `next build` succeeds with all new pages compiled
- `pnpm run test` at monorepo root passes (268+ tests)

## Verification

- `cd apps/web && pnpm next build` exits 0 — all MDX pages compile without syntax errors
- `pnpm run test` at monorepo root — 268+ tests pass (no regressions)
- Build output shows routes for all 5 docs pages
- fumadocs sidebar displays all 5 pages in the correct order
- Content accuracy spot-checks: init prompts match `init-prompts.ts`, config fields match `types.ts`, workflow structure matches `workflows.ts`

## Tasks

- [x] **T01: Write Quick Start, init walkthrough, and configuration reference** `est:45m`
  - Why: These three pages form the "getting started and configuring" documentation arc. Quick Start needs inaccuracies fixed (standalone `generate` command doesn't exist). Init walkthrough expands on the prompt flow. Config reference documents the DriftlessConfig interface.
  - Files: `apps/web/content/docs/index.mdx`, `apps/web/content/docs/init-walkthrough.mdx`, `apps/web/content/docs/configuration.mdx`
  - Do: Rewrite Quick Start to remove `generate` as standalone command, list all 6 init prompts, add Claude Code prerequisite callout. Write init walkthrough showing each prompt step with message/placeholder/default from `init-prompts.ts`, plus what happens after prompts (detection → config → dry-run or write → generate → skills → workflows → debug log → commit). Write config reference with table of every DriftlessConfig field from `types.ts`, framework detection table from `detect.ts`, doc framework adapter descriptions from `adapters.ts`. All pages use fumadocs conventions: YAML frontmatter only, no `# Title`, `<Callout>` for tips/warnings.
  - Verify: `cd apps/web && pnpm next build` exits 0 with new routes visible in output
  - Done when: Three MDX pages compile, content matches source code, cross-links use absolute paths

- [x] **T02: Write GitHub Actions and troubleshooting, wire sidebar ordering** `est:40m`
  - Why: Completes the "operating and debugging" documentation arc. GitHub Actions page covers both workflow templates with operational edges. Troubleshooting covers real error messages from the codebase. `meta.json` orders the sidebar. Final verification confirms the full docs site works.
  - Files: `apps/web/content/docs/github-actions.mdx`, `apps/web/content/docs/troubleshooting.mdx`, `apps/web/content/docs/meta.json`
  - Do: Write GitHub Actions page explaining both workflows (doc-update, test-gen), the ANTHROPIC_API_KEY secret requirement, what each operational edge handler does (fork detection, API key check, bot loop, PR branch checkout, full history). Show annotated workflow structure but note it's auto-generated by `driftless init`. Write troubleshooting page covering: "Config file not found", "Invalid JSON in config file", "Claude Code not found" (spawn error), agent timeout, debug log location and format, rollback behavior. Write `meta.json` with `pages` array ordering all 5 slugs. Run `next build` and `pnpm run test` to verify everything.
  - Verify: `cd apps/web && pnpm next build` exits 0, `pnpm run test` passes 268+ tests, all 5 pages appear in build output
  - Done when: All five docs pages compile, sidebar order is correct, search indexes new content, 268+ tests pass

## Observability / Diagnostics

- **Build verification**: `cd apps/web && pnpm next build` — route list in output confirms all 5 pages compile. Any MDX syntax errors surface as build failures with file paths.
- **Content accuracy**: `grep -c "prompt_message_substring" apps/web/content/docs/init-walkthrough.mdx` confirms source-derived content matches `init-prompts.ts`.
- **Sidebar ordering**: `cat apps/web/content/docs/meta.json` shows page order. Fumadocs uses this to render navigation.
- **Cross-link integrity**: `grep -r '/docs/' apps/web/content/docs/*.mdx` lists all internal links for manual or automated broken-link checks.
- **No secrets or runtime credentials** are involved in documentation pages — no redaction constraints.

## Files Likely Touched

- `apps/web/content/docs/index.mdx` (rewrite)
- `apps/web/content/docs/init-walkthrough.mdx` (new)
- `apps/web/content/docs/configuration.mdx` (new)
- `apps/web/content/docs/github-actions.mdx` (new)
- `apps/web/content/docs/troubleshooting.mdx` (new)
- `apps/web/content/docs/meta.json` (new)
